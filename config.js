/* Auxegen Funding — site configuration
   The lead form sends applications to this URL (your Cloudflare Worker).
   Your StartCap API key is NOT here — it lives only inside the Worker as a secret. */
window.AUXEGEN_LEAD_ENDPOINT = "https://auxegen-lead-proxy.matt-c78.workers.dev";
